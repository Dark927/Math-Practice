using UnityEngine;

public class DrawGraph : MonoBehaviour
{
    //[SerializeField] int graphDelta = 20;

    //Coords minPos = new Coords(-160, -100);
    //Coords maxPos = new Coords(160, 100);

    //float lineWidth = 0.75f;
    //Color lineColor = Color.grey;

    //void Start()
    //{
    //    DrawHorizontalLines();
    //    DrawVerticalLines();
    //}

    //private void DrawHorizontalLines()
    //{
    //    for (float currentLinePosY = 0; currentLinePosY <= maxPos.Y; currentLinePosY += graphDelta)
    //    {
    //        Coords.DrawLine(new Coords(minPos.X, currentLinePosY, 0), new Coords(maxPos.X, currentLinePosY, 0), lineWidth, lineColor);
    //        Coords.DrawLine(new Coords(minPos.X, -currentLinePosY, 0), new Coords(maxPos.X, -currentLinePosY, 0), lineWidth, lineColor);
    //    }
    //}

    //private void DrawVerticalLines()
    //{

    //    for (float currentLinePosX = 0; currentLinePosX <= maxPos.X; currentLinePosX += graphDelta)
    //    {
    //        Coords.DrawLine(new Coords(currentLinePosX, minPos.Y, 0), new Coords(currentLinePosX, maxPos.Y, 0), lineWidth, lineColor);
    //        Coords.DrawLine(new Coords(-currentLinePosX, minPos.Y, 0), new Coords(-currentLinePosX, maxPos.Y, 0), lineWidth, lineColor);
    //    }

    //}
}
